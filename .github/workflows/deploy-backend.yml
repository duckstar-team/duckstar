name: Deploy Backend

on:
  push:
    branches:
      - master
    paths:
      - 'backend/**'
      - 'docker-compose.yml'
      - '.github/workflows/deploy-backend.yml'

# 동시 배포 제어: 같은 EC2 인스턴스에 대한 배포는 순차적으로 실행
concurrency:
  group: deploy-ec2
  cancel-in-progress: false  # 진행 중인 배포는 취소하지 않고 대기

jobs:
  deploy-backend:
    runs-on: ubuntu-latest

    env:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: ${{ secrets.SPRING_DATASOURCE_URL }}
      SPRING_DATASOURCE_USERNAME: ${{ secrets.SPRING_DATASOURCE_USERNAME }}
      SPRING_DATASOURCE_PASSWORD: ${{ secrets.SPRING_DATASOURCE_PASSWORD }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      SECURITY_IP_HASH_KEY: ${{ secrets.SECURITY_IP_HASH_KEY }}
      GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
      GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
      NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
      NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
      KAKAO_CLIENT_ID: ${{ secrets.KAKAO_CLIENT_ID }}
      KAKAO_CLIENT_SECRET: ${{ secrets.KAKAO_CLIENT_SECRET }}
      KAKAO_ADMIN_KEY: ${{ secrets.KAKAO_ADMIN_KEY }}
      NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Discord Notification - 배포 시작
        if: always()
        run: |
          curl -H "Content-Type: application/json" \
               -d '{
                 "embeds": [{
                   "title": "🚀 Backend 배포 시작",
                   "color": 3447003,
                   "fields": [
                     {"name": "브랜치", "value": "${{ github.ref_name }}", "inline": true},
                     {"name": "커밋", "value": "${{ github.sha }}", "inline": false},
                     {"name": "작성자", "value": "${{ github.actor }}", "inline": true}
                   ],
                   "timestamp": "'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"
                 }]
               }' \
               ${{ secrets.DISCORD_WEBHOOK_URL }} || true

      - name: Get public IP of GitHub Actions runner
        id: ip
        run: echo "IP=$(curl -s https://checkip.amazonaws.com)" >> $GITHUB_OUTPUT

      - name: Add IP to EC2 Security Group for SSH
        id: add_ip
        run: |
          # 현재 IP가 이미 보안 그룹에 있는지 확인
          if aws ec2 describe-security-groups \
            --group-ids sg-01655f148522c2cac \
            --query "SecurityGroups[0].IpPermissions[?IpRanges[?CidrIp=='${{ steps.ip.outputs.IP }}/32']]" \
            --output text | grep -q "${{ steps.ip.outputs.IP }}/32"; then
            echo "IP ${{ steps.ip.outputs.IP }} is already in the security group"
            echo "ip_added=false" >> $GITHUB_OUTPUT
          else
            echo "Adding IP ${{ steps.ip.outputs.IP }} to security group"
            aws ec2 authorize-security-group-ingress \
              --group-id sg-01655f148522c2cac \
              --protocol tcp \
              --port 22 \
              --cidr ${{ steps.ip.outputs.IP }}/32
            echo "ip_added=true" >> $GITHUB_OUTPUT
          fi
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ap-northeast-2

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      - name: Create .env file
        run: |
          cat <<EOF > .env
          SPRING_PROFILES_ACTIVE=${{ env.SPRING_PROFILES_ACTIVE }}
          SPRING_DATASOURCE_URL=${{ env.SPRING_DATASOURCE_URL }}
          SPRING_DATASOURCE_USERNAME=${{ env.SPRING_DATASOURCE_USERNAME }}
          SPRING_DATASOURCE_PASSWORD=${{ env.SPRING_DATASOURCE_PASSWORD }}
          JWT_SECRET=${{ env.JWT_SECRET }}
          SECURITY_IP_HASH_KEY=${{ env.SECURITY_IP_HASH_KEY }}
          GOOGLE_CLIENT_ID=${{ env.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET=${{ env.GOOGLE_CLIENT_SECRET }}
          NAVER_CLIENT_ID=${{ env.NAVER_CLIENT_ID }}
          NAVER_CLIENT_SECRET=${{ env.NAVER_CLIENT_SECRET }}
          KAKAO_CLIENT_ID=${{ env.KAKAO_CLIENT_ID }}
          KAKAO_CLIENT_SECRET=${{ env.KAKAO_CLIENT_SECRET }}
          KAKAO_ADMIN_KEY=${{ env.KAKAO_ADMIN_KEY }}
          NEXT_PUBLIC_API_URL=${{ env.NEXT_PUBLIC_API_URL }}
          EOF

      - name: Upload code to EC2
        run: |
          echo "📦 코드 압축 중..."
          tar -czf /tmp/duckstar-code.tar.gz \
            --exclude='.git' \
            --exclude='node_modules' \
            --exclude='backend/build' \
            --exclude='frontend/.next' \
            --exclude='frontend/node_modules' \
            backend/ \
            docker-compose.yml \
            nginx/ \
            .env
          
          echo "📤 EC2로 코드 전송 중..."
          scp -o StrictHostKeyChecking=no /tmp/duckstar-code.tar.gz ubuntu@${{ secrets.EC2_HOST }}:/tmp/duckstar-code.tar.gz
          
          echo "📂 EC2에서 코드 압축 해제 중..."
          ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.EC2_HOST }} <<'EOF'
            set -e
            cd /home/ubuntu/duckstar
            
            # 기존 파일 백업 (선택사항)
            if [ -d "backend" ]; then
              echo "기존 backend 디렉토리 백업 중..."
            fi
            
            # 압축 해제
            tar -xzf /tmp/duckstar-code.tar.gz -C /home/ubuntu/duckstar
            
            # 임시 파일 정리
            rm -f /tmp/duckstar-code.tar.gz
            
            echo "✅ 코드 업데이트 완료"
          EOF
          
          # 로컬 임시 파일 정리
          rm -f /tmp/duckstar-code.tar.gz

      - name: Deploy Backend to EC2
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.EC2_HOST }} <<'EOF'
            set -e
            cd /home/ubuntu/duckstar

            echo "🧹 빌드 공간 확보: 오래된 도커 데이터 정리 (빌드 전)"
            # 사용하지 않는 컨테이너, 네트워크, 이미지 모두 삭제 (실행 중인 건 건드리지 않음)
            docker system prune -af
            # 빌드 캐시 삭제 (이게 용량을 가장 많이 차지함)
            docker builder prune -af

            echo "🛠 백엔드 빌드 시작"
            # 이제 깨끗한 상태에서 빌드
            docker compose up -d --build backend

            echo "📋 컨테이너 상태"
            docker ps -a

            echo "📄 백엔드 최근 로그"
            docker compose logs --tail=50 backend
          EOF

      - name: Health Check
        id: health_check
        run: |
          echo "🏥 헬스체크 시작..."
          MAX_ATTEMPTS=12
          ATTEMPT=0
          
          while [ $ATTEMPT -lt $MAX_ATTEMPTS ]; do
            ATTEMPT=$((ATTEMPT + 1))
            echo "헬스체크 시도 $ATTEMPT/$MAX_ATTEMPTS..."
            
            # backend 컨테이너 안에서 직접 헬스체크
            if ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.EC2_HOST }} \
              "docker exec duckstar-backend curl -f -s http://backend:8080/actuator/health > /dev/null 2>&1"; then
              echo "✅ 헬스체크 성공!"
              echo "status=success" >> $GITHUB_OUTPUT
              exit 0
            fi
            
            if [ $ATTEMPT -lt $MAX_ATTEMPTS ]; then
              echo "⏳ 서비스 준비 중... 5초 대기"
              sleep 5
            fi
          done
          
          echo "❌ 헬스체크 실패: 60초 내에 서비스가 응답하지 않음"
          echo "status=failed" >> $GITHUB_OUTPUT
          exit 1

      - name: Discord Notification - 배포 성공
        if: steps.health_check.outputs.status == 'success'
        run: |
          curl -H "Content-Type: application/json" \
               -d '{
                 "embeds": [{
                   "title": "✅ Backend 배포 성공",
                   "color": 3066993,
                   "fields": [
                     {"name": "브랜치", "value": "${{ github.ref_name }}", "inline": true},
                     {"name": "커밋", "value": "[${{ github.sha }}](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})", "inline": false},
                     {"name": "작성자", "value": "${{ github.actor }}", "inline": true},
                     {"name": "헬스체크", "value": "✅ 통과", "inline": true}
                   ],
                   "timestamp": "'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"
                 }]
               }' \
               ${{ secrets.DISCORD_WEBHOOK_URL }} || true

      - name: Discord Notification - 배포 실패
        if: failure()
        run: |
          curl -H "Content-Type: application/json" \
               -d '{
                 "embeds": [{
                   "title": "❌ Backend 배포 실패",
                   "color": 15158332,
                   "fields": [
                     {"name": "브랜치", "value": "${{ github.ref_name }}", "inline": true},
                     {"name": "커밋", "value": "[${{ github.sha }}](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})", "inline": false},
                     {"name": "작성자", "value": "${{ github.actor }}", "inline": true},
                     {"name": "워크플로우", "value": "[상세 보기](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})", "inline": false}
                   ],
                   "timestamp": "'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"
                 }]
               }' \
               ${{ secrets.DISCORD_WEBHOOK_URL }} || true

      - name: Remove IP from EC2 Security Group
        if: always() && steps.add_ip.outputs.ip_added == 'true'
        run: |
          echo "Removing IP ${{ steps.ip.outputs.IP }} from security group"
          aws ec2 revoke-security-group-ingress \
            --group-id sg-01655f148522c2cac \
            --protocol tcp \
            --port 22 \
            --cidr ${{ steps.ip.outputs.IP }}/32 || echo "Failed to remove IP (may have been removed already)"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ap-northeast-2

